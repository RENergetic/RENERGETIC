apiVersion: apps/v1
kind: Deployment
metadata:
  name: keycloak
spec:
  replicas: 1
  selector:
    matchLabels:
      app: keycloak
  template:
    metadata:
      labels:
        app: keycloak
    spec:
      containers:
      - name: keycloak
        image: docker-registry.default.svc:5000/ren-prototype/keycloak:latest
        imagePullPolicy: Always
        env:
        - name: KEYCLOAK_USER
          value: "admin"
        - name: KEYCLOAK_PASSWORD
          value: "admin"
        - name: PROXY_ADDRESS_FORWARDING
          value: "true"
        - name: DB_VENDOR
          value: "POSTGRES"
        - name: DB_ADDR
          value: "postgresql-db-sv"
        - name: DB_PORT
          value: "5432"
        - name: DB_SCHEMA
          value: "public"
        - name: DB_DATABASE
          value: "renerdb"
        - name: DB_USER
          value: "postgres"
        - name: DB_PASSWORD
          value: "postgres"
        ports:
        - name: http
          containerPort: 8080
        - name: https
          containerPort: 8443
        resources:
          requests:
            cpu: 250m
            memory: 600Mi
          limits:
            cpu: 800m
            memory: 700Mi
#        volumeMounts:
#        - name: keycloak-diskc
#          mountPath: /opt/jboss/keycloak/standalone/configuration/
        readinessProbe:
          httpGet:
            path: /auth/realms/master
            port: 8080
      restartPolicy: Always
#      volumes:
#        - name: keycloak-diskc
#          persistentVolumeClaim:
#            claimName: keycloak-disk