apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-ini
data:
  grafana.ini: |
    [security]
      allow_embedding = true
    [auth.anonymous]
      enabled = true
      org_name = external
      org_role = Viewer
      hide_version = true